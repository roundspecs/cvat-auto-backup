CVAT_HOST=http://100.100.100.100:8080
CVAT_USERNAME=your_username
CVAT_PASSWORD=your_password
CVAT_PROJECT_ID=1
SAVE_DIR=./backups
# Optional: if you want the script to push the extracted backup into a separate git repo
# set these values. GIT_TOKEN should be a personal access token with repo write permissions.
# Do NOT commit real tokens to version control; keep them in a local .env file or pass via CLI.
# Example target repo format: https://github.com/owner/target-repo.git
TARGET_REPO=
TARGET_BRANCH=main
GIT_USERNAME=your_github_username
GIT_TOKEN=ghp_xxx